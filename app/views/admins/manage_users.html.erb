<%= button_to "Sign out", destroy_admin_session_path, method: :delete %>

<br>

<%= link_to "Accueil Admin", root_path %>

<br><br>

<%= link_to 'Retour', :back %>

<h1>Index Administrateurs</h1>
<p>Membres existants</p>
<ul>
    <% @users.each do |user| %>
        <li>
            <%= link_to user.email, edit_user_path(user.id) %>
        </li>
    <% end %>
</ul>
